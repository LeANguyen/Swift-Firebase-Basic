# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'Firebase Crash Course' do
  # Comment the next line if you don't want to use dynamic frameworks
  # use_frameworks!
  use_modular_headers!

  # Pods for Firebase Crash Course
  pod 'Firebase/Core'
  pod 'Firebase/Database'
  pod 'Firebase/Analytics'
  pod 'Firebase/Auth'
  pod 'Firebase/Storage'

  # Pods for Facebook
  pod 'FBSDKLoginKit'

  # Pods for Google Sign In
  pod 'GoogleSignIn'

  # Pods for ImagePicker 
  pod 'ImagePicker'

  target 'Firebase Crash CourseTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'Firebase Crash CourseUITests' do
    # Pods for testing
  end

end
